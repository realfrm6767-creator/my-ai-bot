"""
settings.py
-----------
مسئول خواندن و نوشتن فایل settings.json (تنظیمات پویای پروژه).

مقادیر ذخیره‌شده: admins, provider, model, language, memory, temperature

توابعی که در مرحله هفتم پیاده‌سازی می‌شوند:
    - load_settings()
    - save_settings(data)
    - get_setting(key)
    - update_setting(key, value)
"""